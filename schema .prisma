# Social Media Platform Database Schema

## Overview
This schema is designed for the Phase 1 social media features and Phase 2 database migration:
- User authentication
- User profiles
- News feed
- Create/delete/view posts
- Likes
- Comments
- Follow/unfollow
- Seed data and statistics queries

## Recommended Database
Use **SQLite** for simplicity during development, then switch to PostgreSQL later if needed.

## Main Entities
- User
- Post
- Comment
- Like
- Follow

## Prisma Schema (`prisma/schema.prisma`)
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id             Int       @id @default(autoincrement())
  username       String    @unique
  email          String    @unique
  password       String
  role           String    @default("USER")
  profilePicture String?
  bio            String?
  createdAt      DateTime  @default(now())

  posts          Post[]
  comments       Comment[]
  likes          Like[]

  following      Follow[]  @relation("UserFollowing")
  followers      Follow[]  @relation("UserFollowers")
}

model Post {
  id          Int        @id @default(autoincrement())
  content     String
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt

  authorId    Int
  author      User       @relation(fields: [authorId], references: [id], onDelete: Cascade)

  comments    Comment[]
  likes       Like[]
}

model Comment {
  id          Int       @id @default(autoincrement())
  content     String
  createdAt   DateTime  @default(now())

  authorId    Int
  postId      Int

  author      User      @relation(fields: [authorId], references: [id], onDelete: Cascade)
  post        Post      @relation(fields: [postId], references: [id], onDelete: Cascade)
}

model Like {
  createdAt   DateTime  @default(now())

  userId      Int
  postId      Int

  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  post        Post      @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@id([userId, postId])
}

model Follow {
  createdAt     DateTime  @default(now())

  followerId    Int
  followingId   Int

  follower      User      @relation("UserFollowing", fields: [followerId], references: [id], onDelete: Cascade)
  following     User      @relation("UserFollowers", fields: [followingId], references: [id], onDelete: Cascade)

  @@id([followerId, followingId])
}
```

## Why these tables?
### User
Stores account and profile data.

### Post
Stores each message/post written by a user.

### Comment
Stores comments on posts.

### Like
Represents a many-to-many relationship between users and posts.
The unique constraint prevents one user from liking the same post twice.

### Follow
Represents a self-referencing many-to-many relationship between users.
One user follows another user.

## Relationship Summary
- One **User** can create many **Posts**
- One **User** can create many **Comments**
- One **User** can create many **Likes**
- One **Post** can have many **Comments**
- One **Post** can have many **Likes**
- One **User** can follow many users
- One **User** can be followed by many users

## Notes for your report
You can describe this schema as:
> A relational database schema for a social media platform, implemented with Prisma ORM. The schema supports authentication, user profiles, posts, comments, likes, and follower relationships. Junction tables are used for likes and follows to model many-to-many relationships.

## Next commands
```bash
npm install prisma @prisma/client
npx prisma init
npx prisma migrate dev --name init
npx prisma studio
```

## `.env`
```env
DATABASE_URL="file:./dev.db"
```

## Good Phase 2 statistics you can later build
1. Average number of followers per user
2. Average number of posts per user
3. Most active user by number of posts
4. Most liked post
5. User with the most followers
6. Number of comments per post
